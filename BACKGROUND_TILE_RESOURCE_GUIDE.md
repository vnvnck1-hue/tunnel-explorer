# 땅굴 크루 배경 타일 리소스 제작 규격 가이드

기준 파일: `tunnel-crew-loop-demo-v2-hud-polished.html`

이 문서는 현재 게임의 `blockSprite()` 생성 규칙, `genTunnel()` 타일 배치 규칙, `drawDepths()` 합성 좌표를 기준으로 작성했다. 아래에서 **런타임 규격**은 현재 코드와 정확히 맞아야 하는 값이고, **제작 권장 규격**은 AI 이미지 생성과 후처리를 안정적으로 하기 위한 원본 규격이다.

## 1. 가장 중요한 결론

| 항목 | 정확한 규격 |
|---|---:|
| 논리 타일 크기 | **50 × 50 world px** |
| 최종 개별 타일 PNG | **50 × 50 px, RGBA PNG** |
| AI 제작용 마스터 권장 | **100 × 100 px/타일 (@2x)** |
| 타일 피벗 | 좌상단 `(0, 0)` |
| 그리드 원점 | 맵 좌상단 |
| 타일 월드 위치 | `(column × 50, row × 50)` |
| 손상 타일 투명도 | 가장자리 결손부는 반드시 alpha 0 |
| 권장 샘플링 | @2x 제작 후 50 × 50으로 고품질 축소, 런타임은 bilinear 허용 |

현재 렌더러는 `CELL = 50`으로 모든 벽과 바닥을 배치한다. 내부 캔버스는 `round(50 × devicePixelRatio)`로 만들지만, 최종 월드에서는 항상 `50 × 50`으로 그린다. 따라서 DPR은 고해상도 렌더링용일 뿐, 게임 리소스의 논리 크기를 바꾸지 않는다.

## 2. 타일 레이어 구조

게임 화면은 아래 순서로 합성된다.

1. 암흑 배경
2. 바닥 타일
3. 바닥 데칼과 잔해
4. 벽 타일
5. `core` 노출 측면과 윗면
6. 깊이 밴드 경계선
7. 출구, 소품, 랜턴, 캐릭터
8. 조명과 FoW

따라서 AI로 만들어야 할 핵심 배경 리소스는 다음 네 묶음이다.

- `floor`: 빈 공간에 깔리는 바닥
- `wall`: 채굴 가능한 벽과 광맥
- `solid`: 파괴 불가 암반인 `rock`, `core`
- `overlay`: core 측면, 깊이 경계선, 천장 돌출부, 바닥 데칼

## 3. 타일 종류

### 3.1 바닥

| ID | 용도 | 크기 | 불투명도 |
|---|---|---:|---|
| `floor_dark` | 어두운 바닥 변형 | 50 × 50 | 불투명 |
| `floor_base` | 기본 바닥 | 50 × 50 | 불투명 |
| `floor_rim` | 밝은 바닥 변형 | 50 × 50 | 불투명 |

현재 코드는 좌표 해시 `((column×17 + row×31) & 7) / 7`로 세 변형을 결정한다. 결과 분포는 `floor_dark` 25%, `floor_base` 25%, `floor_rim` 50%다. 각 타일은 가장자리까지 완전히 채워야 한다. 코드가 틈 방지를 위해 실제로 `50.6 × 50.6`까지 덮지만, 이미지 파일은 정확히 50 × 50으로 만든다.

바닥에는 현재 다음 디테일이 추가된다.

- 약 1px의 안쪽 테두리
- 일부 칸의 중앙 부근에 폭 70%, 높이 6px 정도의 어두운 가로 흔적
- 별도 바닥 데칼: 둥근 얼룩 또는 룬

바닥 데칼은 바닥에 굽지 말고 RGBA 오버레이로 분리하는 것을 권장한다.

### 3.2 채굴 가능한 벽

| ID | 의미 | 기본 HP | 보상 | 깊이 밴드 색 적용 | 손상 4단계 |
|---|---|---:|---|---|---|
| `dirt` | 약한 일반 벽 | 80 | pulp 1 | 적용 | 적용 |
| `stone` | 중간 강도 벽 | 140 | pulp 2 | 색상에는 미적용 | 적용 |
| `ore` | 일반 광맥 | 100 | bloom 1 | 적용 | 적용 |
| `gem` | 고가치 광맥 | 100 | bloom 3 | 적용 | 적용 |
| `crys` | 결정 광맥 | 100 | bloom 2 | 적용 | 적용 |

`ore`, `gem`, `crys`는 별도의 바닥 오브젝트가 아니라 **벽 타일 자체**다. 일반 벽 바탕 위에 세 개의 결정 군집이 포함된다.

현재 결정의 대략적 중심과 크기는 50px 타일 기준 다음과 같다.

| 결정 | 중심 | 높이 | 반폭 | 회전 |
|---|---:|---:|---:|---:|
| 주 결정 | `(26, 29)` | 17px | 7px | -0.08rad |
| 좌하 결정 | `(15, 35)` | 10px | 4px | 0.35rad |
| 우상 결정 | `(36, 19)` | 11px | 4.5px | -0.40rad |

광맥 식별 실루엣은 손상 단계가 바뀌어도 유지해야 한다. 가장자리 결손과 균열만 증가시키고 결정 군집의 중심은 이동시키지 않는다.

### 3.3 파괴 불가 타일

| ID | 의미 | 크기 | 손상 단계 | 비고 |
|---|---|---:|---|---|
| `rock` | 맵 외곽 봉쇄 암반 | 50 × 50 | 없음 | 드릴이 튕김 |
| `core_top` | 내부 파괴 불가 코어 윗면 | 50 × 50 | 없음 | 노출 측면과 조합 |

두 타일의 HP는 사실상 무한대(`1e9`)이며 손상 프레임을 사용하지 않는다.

`rock`은 색상 자체는 깊이 밴드와 무관하지만 현재 절차 텍스처의 난수 패턴이 밴드별로 달라진다. 완전한 외형 대응이 필요하면 바이옴당 4장을 만들고, 제작량을 줄이려면 1장을 모든 밴드에서 공유해도 게임 규칙에는 영향이 없다.

## 4. 손상 단계의 정확한 규칙

손상 인덱스는 다음 식으로 정해진다.

```text
damageRatio = 1 - currentHP / maxHP
damageStage = clamp(round(damageRatio × 3), 0, 3)
```

| 단계 | 표시되는 손상률 | 외형 규칙 |
|---:|---:|---|
| 0 | 0% 이상, 16.67% 미만 | 온전한 타일 |
| 1 | 16.67% 이상, 50% 미만 | 기본 균열 1세트, 가장자리 결손 3개 |
| 2 | 50% 이상, 83.33% 미만 | 추가 균열, 가장자리 결손 6개 |
| 3 | 83.33% 이상, 100% 미만 | 가장자리 결손 9개, 큰 모서리 결손 1개 |
| 파괴 | 100% | 타일 제거, 바닥 노출 |

손상 단계 1~3의 결손부는 검은색으로 칠하는 것이 아니라 **투명 픽셀**이어야 한다. 아래 바닥이 그대로 보여야 현재 렌더러와 같은 결과가 난다.

## 5. 표면 변형 단계

일반 타일은 손상과 별개로 세 가지 표면 변형을 갖는다.

| `surfaceVariant` | 의미 | 생성 확률 |
|---:|---|---:|
| 0 | 장식 없음 | 80% |
| 1 | 자갈/광물 얼룩 | 14% |
| 2 | 룬/낙서 | 6% |

이 값은 `dirt`, `stone`, `ore`, `gem`, `crys`에 모두 배정된다. 현재 코드는 변형을 타일 이미지 안에 굽지만, 신규 아트 파이프라인에서는 아래 두 오버레이로 분리하는 편이 일관성과 제작량 측면에서 유리하다.

- `decal_pebble_00..N`: 50 × 50 RGBA
- `decal_rune_00..N`: 50 × 50 RGBA

오버레이를 분리하더라도 최종 조합 의미는 `surfaceVariant 0/1/2`를 그대로 보존한다.

## 6. 깊이 밴드

각 바이옴에는 정확히 4개의 깊이 색상 밴드가 있다.

```text
bandRows = ceil(mapRows / 4)
band = clamp(floor(row / bandRows), 0, 3)
```

현재 기본 데모 맵은 80 × 72 타일이므로:

| 밴드 | 행 범위 | 월드 Y 범위 | 높이 |
|---:|---:|---:|---:|
| 0 | 0–17 | 0–899px | 18타일 / 900px |
| 1 | 18–35 | 900–1799px | 18타일 / 900px |
| 2 | 36–53 | 1800–2699px | 18타일 / 900px |
| 3 | 54–71 | 2700–3599px | 18타일 / 900px |

밴드 경계에는 높이 6px의 수평 오버레이가 들어가며, 맵 좌우 끝 한 타일씩을 제외하고 그려진다. 터널 모드에서는 alpha 0.30으로 합성한다.

깊이 밴드는 미션 층 `L1`, `L2`와 다른 개념이다. 미션 층이 바뀌어도 한 맵 안에는 다시 4개 밴드가 존재한다.

## 7. 바이옴과 정확한 팔레트

### 7.1 퍼플 땅굴 (`purple`, 현재 기본/해금)

| 요소 | band 0 | band 1 | band 2 | band 3 |
|---|---|---|---|---|
| `dirt` base | `#2E2248` | `#352851` | `#2A1E44` | `#241A3C` |
| `dirt` highlight | `#3D2E5E` | `#463568` | `#382A58` | `#322650` |
| `dirt` shadow | `#1A1430` | `#211A38` | `#161028` | `#120E24` |

공통 색상:

| 요소 | base | highlight | shadow |
|---|---|---|---|
| `stone` | `#3A2E58` | `#4E3E70` | `#2A2040` |
| `rock` | `#161C32` | `#6A7EB0` | `#080C18` |
| `core` 기준 | `#1E2438` | `#7A8CB8` | `#0E121C` |
| 바닥 | `#6A5744` | rim `#8A7460` | dark `#534334` |
| 광맥 | ore `#F06AB8` 계열 | gem `#FF4FA8` | crys `#5EE8FF` |

밴드 경계색은 순서대로 `#6B3A8A`, `#4A2868`, `#2A1848`이다.

### 7.2 청록 염굴 (`brine`, UI에서는 현재 잠금)

| 요소 | band 0 | band 1 | band 2 | band 3 |
|---|---|---|---|---|
| `dirt` base | `#1A3840` | `#1E4048` | `#163440` | `#123038` |
| `dirt` highlight | `#254850` | `#2A545C` | `#224850` | `#1E4048` |
| `dirt` shadow | `#102830` | `#143038` | `#0E2830` | `#0C2028` |

공통 색상:

| 요소 | base | highlight | shadow |
|---|---|---|---|
| `stone` | `#2A5058` | `#3A6870` | `#1A3840` |
| `rock` | `#0A1824` | `#4A90A8` | `#040C14` |
| `core` 기준 | `#0E2030` | `#5AA8C0` | `#061018` |
| 바닥 | `#3A554C` | rim `#5A7868` | dark `#2C423A` |
| 광맥 | ore `#5EE8D0` 계열 | gem `#3AD0C0` | crys `#7FFFF0` |

밴드 경계색은 순서대로 `#2A8890`, `#1A6068`, `#0E4048`이다.

## 8. `core` 타일의 특수 규격

`core`는 일반 50 × 50 타일처럼 한 번만 그려지지 않는다.

### 정확한 합성

- `core_top`: 50 × 50
- 윗면 배치 위치: 논리 셀 좌상단보다 **13px 위**, 즉 `(x, y - 13)`
- 노출 측면 높이: **13px** (`50 × 0.26`)
- 측면 배치 범위: 셀 안의 `y + 37`부터 `y + 50`
- 측면은 바로 아래 칸이 `core`가 아닐 때만 표시
- 맨 위가 노출된 core에는 윗면 상단 2.4px 하이라이트
- 좌측 노출 가장자리: 2.2px 어두운 선
- 우측 노출 가장자리: 2.2px 밝은 선

따라서 `core_top`에 측면을 굽지 않는다. 필요한 리소스는 다음과 같다.

| 리소스 | 크기 | 개수/바이옴 | 조건 |
|---|---:|---:|---|
| `core_top_b{0..3}_v{0..5}` | 50 × 50 | 24 | 항상 |
| `core_side_b{0..3}` | 50 × 13 | 4 | 아래쪽이 노출될 때 |
| `core_bottom_shadow` | 50 × 11 | 1 | 아래쪽이 노출될 때, 선택 |

현재 core 윗면은 밴드당 6개의 결정면 변형을 사용한다. `variant = (column×7 + row×13) mod 6`으로 선택한다.

만약 한 장짜리 합성 core를 꼭 써야 한다면 아트 캔버스는 50 × 63으로 만들고, 논리 셀 기준 피벗을 `(0, 13)`에 둔다. 하지만 세로로 이웃한 core 사이에도 측면이 보이는 문제가 생길 수 있으므로, **윗면과 측면 분리 방식이 정확한 구현**이다.

## 9. 현재 맵 단계와 크기

### 9.1 터널 미션 표기 단계

코드에는 다음 표가 존재한다.

| 미션 층 | 표기 | 의도된 타일 크기 | 의도된 월드 크기 |
|---:|---|---:|---:|
| L1 | S | 24 × 20 | 1200 × 1000px |
| L2 | M | 30 × 26 | 1500 × 1300px |
| L3 | L | 36 × 32 | 1800 × 1600px |
| L4 | XL | 44 × 38 | 2200 × 1900px |
| L5 | XXL | 52 × 44 | 2600 × 2200px |
| L6+ | XXL+ | 64 × 54 | 3200 × 2700px |

단, **현재 첨부 데모의 실제 터널 맵 크기는 미션 층과 무관하게 `DEMO.mapW = 80`, `DEMO.mapH = 72`를 사용한다.** 즉 실제 기본 월드 크기는 **4000 × 3600px**다. 위 표는 현재 코드에서 `S/M/L...` 라벨 계산에는 쓰이지만 실제 `setLayerSize()`에는 반영되지 않는다. 리소스 제작은 미션 맵 크기가 아니라 50 × 50 타일 규격만 따르면 모든 단계에 공용할 수 있다.

### 9.2 튜토리얼 진행 단계

스토리 학습 단계 0~3은 일반 채굴 모드의 맵 크기와 규칙을 바꾸지만, 타일 한 장의 크기나 타입은 바꾸지 않는다. 배경 타일 시트를 단계별로 따로 만들 필요는 없다.

## 10. 엄격한 1:1 대응 시트 규격

현재 절차 변형까지 개별 이미지로 치환하려면 **바이옴당 268장의 50 × 50 벽 타일**이 필요하다.

### 개수

| 묶음 | 계산 | 개수 |
|---|---:|---:|
| dirt | 4 bands × 3 surface × 4 damage | 48 |
| stone | 4 bands × 3 surface × 4 damage | 48 |
| ore | 4 × 3 × 4 | 48 |
| gem | 4 × 3 × 4 | 48 |
| crys | 4 × 3 × 4 | 48 |
| rock | 4 bands | 4 |
| core_top | 4 bands × 6 variants | 24 |
| 합계 |  | **268** |

`stone`과 `rock`은 색상을 밴드별로 공유하지만 현재 절차 노이즈 패턴이 밴드에 따라 달라지므로, 외형까지 1:1로 보존할 때는 위 개수를 사용한다.

### 시트 크기와 좌표 공식

한 바이옴의 엄격 대응 시트:

- 최종 시트: **800 × 850px**
- 그리드: **16열 × 17행**
- 셀: **50 × 50px**
- 사용 셀: 268
- 마지막 4셀: 투명/미사용
- AI 마스터 시트(@2x): **1600 × 1700px**, 셀 100 × 100
- 시트 자체에는 간격과 거터를 넣지 않는다. 분리 후 엔진 atlas packer에서 2px extrusion을 추가한다.

인덱스와 좌표:

```text
column = index mod 16
row    = floor(index / 16)

@1x crop = (column×50, row×50, 50, 50)
@2x crop = (column×100, row×100, 100, 100)
```

일반 벽 인덱스:

```text
typeIndex: dirt=0, stone=1, ore=2, gem=3, crys=4
index = typeIndex×48 + band×12 + surfaceVariant×4 + damageStage
```

파괴 불가 타일 인덱스:

```text
rock:     index = 240 + band
core_top: index = 244 + band×6 + variant
```

파일명 예:

```text
purple_dirt_b0_s0_d0.png
purple_dirt_b3_s2_d3.png
purple_gem_b2_s1_d1.png
purple_rock_b1.png
purple_core_top_b3_v5.png
```

## 11. 실제 제작에 권장하는 모듈식 시트

AI가 268개의 셀을 한 번에 정확하게 유지하기는 어렵다. 실제 제작에서는 재질, 손상, 데칼을 분리하면 품질과 일관성이 좋아진다.

권장 구성:

1. `base_wall`: 재질 × 밴드의 온전한 50 × 50 타일
2. `damage_mask_d1..d3`: 50 × 50 alpha 마스크
3. `surface_decal_pebble/rune`: 50 × 50 RGBA
4. `core_top`과 `core_side`: 별도 시트
5. `floor`: 3변형 별도 시트

이 경우 손상 마스크는 재질에 공용할 수 있지만, 광맥 결정이 마스크에 의해 지나치게 잘리지 않도록 재질별 최종 합성 검수가 필요하다. 런타임 의미는 엄격 대응 시트와 동일하게 유지하되, 최종 atlas는 빌드 단계에서 조합한다.

## 12. AI 이미지 생성 지시 시 반드시 고정할 조건

- 정사각형 정면 투영, 원근 없음
- 타일 한 칸은 정확히 100 × 100px 마스터
- 모든 타일의 조명 방향은 위쪽/약간 좌상단으로 통일
- 외곽선이나 큰 형태가 셀 경계를 넘어가지 않음
- 온전한 벽은 네 가장자리까지 불투명하게 채움
- 손상 타일의 결손은 투명 배경
- 이음매 없는 반복을 위해 좌우 색과 명도 범위를 맞춤
- 광맥의 중심 위치와 실루엣은 손상 단계 전체에서 고정
- 글자, 숫자, 워터마크, 시트 구분선 금지
- 셀 간 여백 금지; 후처리 스크립트가 정수 좌표로 자를 수 있어야 함
- 색공간 sRGB, PNG-24/RGBA, premultiplied alpha 금지

AI가 만든 시트는 바로 사용하지 말고, 반드시 정수 그리드로 분리한 뒤 각 셀을 100 × 100인지 검사하고 50 × 50으로 축소한다.

## 13. 후처리와 검수 체크리스트

- [ ] 모든 최종 타일이 정확히 50 × 50인가
- [ ] 손상 타일이 검은 배경이 아닌 투명 alpha를 갖는가
- [ ] `d0 → d3`에서 재질 정체성은 유지되고 손상만 증가하는가
- [ ] 같은 광맥의 결정 중심이 단계별로 움직이지 않는가
- [ ] 좌우/상하로 5 × 5 반복했을 때 밝은 선이나 틈이 생기지 않는가
- [ ] core 윗면이 `(x, y-13)`에 놓이고 측면이 13px인가
- [ ] core 측면이 아래쪽 노출면에만 나타나는가
- [ ] 바닥 3종이 벽의 투명 결손부 아래에서 자연스럽게 보이는가
- [ ] 밴드 0→3 전환 시 명도와 색상 흐름이 갑자기 끊기지 않는가
- [ ] 2px extrusion을 넣은 packed atlas에서도 UV bleed가 없는가
- [ ] FoW와 보라/청록 조명 아래에서도 dirt, stone, ore가 구분되는가

## 14. 범위에서 제외해야 할 것

다음은 배경 타일 시트에 합치지 않는 것이 좋다.

- 랜턴
- 수정 장식 소품
- 발판/비계, 상자, 광차, 거미줄
- 출구 X 마커
- 캐릭터와 적
- 잔해와 채굴 파편 VFX
- 조명, 안개, FoW
- 맵 외곽의 13px 절벽 테두리

이들은 타일 그리드 위에 독립적으로 배치되거나 절차적으로 그려지는 오브젝트다.

